{% extends 'base.html.twig' %}

{% block title %}Edit Category
{% endblock %}

{% block body %}
	<h1>Edit Category</h1>

	{{ form_start(form) }}
	{{ form_widget(form) }}
	<button class="btn btn-success">Update</button>
	{{ form_end(form) }}

	<a href="{{ path('admin_categories') }}" class="btn btn-secondary">Back</a>
{% endblock %}

