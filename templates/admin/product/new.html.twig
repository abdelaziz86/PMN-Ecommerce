{% extends 'base.html.twig' %}

{% block title %}New Product
{% endblock %}

{% block body %}
	<h1>Create New Product</h1>

	{{ form_start(form) }}
	{{ form_widget(form) }}
	<button class="btn btn-success">Create</button>
	{{ form_end(form) }}

	<a href="{{ path('admin_products') }}" class="btn btn-secondary">Back</a>
{% endblock %}

